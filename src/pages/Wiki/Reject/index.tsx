import React from 'react';
import { PageHeaderWrapper } from '@ant-design/pro-layout';

const Reject: React.FC = (): React.ReactElement => {
  return <PageHeaderWrapper>dsadsa</PageHeaderWrapper>;
};

export default Reject;
